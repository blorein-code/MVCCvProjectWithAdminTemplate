@using MVCCvProject.Models.Entity
@model List<HobbyTable>

<div class="resume-section-content">
    <h2 class="mb-5">Hobilerim</h2>
    @foreach (var x in Model)
    {
        <p>@x.Description1</p>
        
    }


</div>
